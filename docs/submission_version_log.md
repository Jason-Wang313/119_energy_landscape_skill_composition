# Submission Version Log

## Superseded Local Builds

- Earlier archive and local-continuation builds were superseded by the v5 expansion.
- Their useful role is now historical only: they established the paper topic, rough mechanism, and prior proposed baseline.
- Their old page counts, hashes, row counts, and baseline statistics should not be used for the current paper state.

## v5_expanded

- Rebuilt the method as `barrier_certified_energy_composer_v5`.
- Added basin-overlap posterior checks, barrier/descent seam tests, terminal-state sampling, high-energy seam repair, contact/dynamics guards, calibration, fixed-risk acceptance, and repair memory.
- Expanded to 12 methods, 10 paired seeds, 230,400 main cells, 38,400 ablation cells, 161,280 stress cells, 107,520 fixed-risk cells, and 24 failure cases.
- Selected the strongest non-oracle baseline from the hard aggregate; it is `proposed_energy_landscape_composer_v4_1`.
- Reported hard success, hard utility, mechanism diagnostics, ablations, stress endpoints, fixed-risk coverage/breach/gated success, and scope blockers.
- Generated a 30-page ICLR-style PDF with hidden link borders and conference-style citations.
- Retitled the main paper to `Local World/Action Models for Robot Skill Seams` so the agenda is explicit in the title while the abstract and scope boundary keep the claim local and testable.
- Updated the manuscript generator to emit a natural skill-seam world/action-interface framing instead of an internal audit abstract.
- Reframed the core claim around predicting handoff failure, diagnosing the failure reason, choosing repair/probe/abstain/transition decisions, updating planner edge beliefs, and feeding those outcomes back into future planning.
- Added a vector-first figure pass with a skill-seam overview diagram and PDF/PNG companion outputs for all main figures.
- Added targeted related-work coverage for CoStream, OAT, SIMPACT, PoCo, Yilun energy-based modeling, and the 2026 robot world-model survey.
- Expanded the related-work boundary with CEP, Diffusion Policy, runtime skill composition, EzSkiROS, latent robot skills, and language/action compositionality, plus `docs/related_work_coverage_matrix.md`.
- Added `docs/independent_validation_protocol.md` as the gate for making the paper independently submission-ready without Haonan.
- Added `docs/haonan_yilun_outreach_package.md` with a CoStream-centered email package and collaboration boundary.
- Added reproducible outreach artifacts: `outreach/paper119_one_page_memo.pdf` and `outreach/paper119_four_page_preview.pdf`.
- Added `scripts/build_outreach_artifacts.ps1` and `scripts/validate_outreach_artifacts.py` for page-count and framing checks.
- Added `external_validation/manifest_template.json`, `external_validation/README.md`, and `scripts/audit_external_evidence.py` so the missing real/high-fidelity validation layer is a machine-checkable submission gate rather than prose.
- Added `scripts/build_external_collection_plan.py` so the independent high-fidelity validation route has a concrete non-evidence schedule before real rollouts are collected.
- Added `scripts/build_external_analysis_plan.py` so the external validation route has a non-evidence preregistered statistical analysis plan that locks hypotheses, schema thresholds, paired-comparison keys, exclusion/unblinding policy, strict gates, and reporting before real rollouts are collected.
- Added `scripts/probe_external_platform.py` so the selected public-simulator machine can record Python, package, GPU/renderer, code-commit, and config/backend hash provenance while strict fidelity and external evidence remain false.
- Added `external_validation/maniskill_task_bindings.json` and `scripts/probe_maniskill_task_bindings.py` so the primary ManiSkill/SAPIEN route binds Paper 119 task families to concrete public-simulator environment candidates while accepted task binding and strict evidence remain false.
- Added `scripts/probe_maniskill_env_smoke.py` so bound ManiSkill/SAPIEN candidates can be construction/reset smoke-tested and missing assets are visible before backend qualification while strict evidence remains false.
- Hardened `scripts/probe_maniskill_env_smoke.py` to isolate each candidate environment in its own subprocess and report required/missing ManiSkill asset IDs; after installing the PartNet cabinet asset locally, the latest non-evidence smoke report shows all four primary ManiSkill/SAPIEN candidates construct/reset while support candidates still require `oakink-v2` and `ycb`.
- Added `scripts/probe_maniskill_fidelity_metadata.py` so the bound ManiSkill/SAPIEN candidates record sim/control timestep, SAPIEN backend, controller, observation/info-key, agent, and asset metadata for fidelity review while accepted fidelity and strict evidence remain false.
- Added `scripts/build_external_platform_onboarding.py` so the primary non-Haonan ManiSkill/SAPIEN route has a non-evidence platform onboarding packet with official source anchors, required simulator provenance, task onboarding files, backend requirements, and strict gate order before external collection.
- Added `scripts/build_external_fidelity_provenance_packet.py` so platform physics/contact, paired-reset replay, operator independence, calibration basis, code/skill hashes, manifest declaration, and strict fidelity gating are converted into a non-evidence external fidelity provenance packet while strict fidelity evidence remains false.
- Added `scripts/build_external_fidelity_acceptance_draft.py` so the tracked ManiSkill route has a draft-only acceptance file prefilled with platform/backend/config/task-binding anchors while all real fidelity gates remain unaccepted.
- Updated the fidelity acceptance draft with a fidelity acceptance promotion checklist that reports machine-prefilled readiness separately from independent operator signoff readiness while keeping promotion and acceptance false.
- Added a strict fidelity acceptance provenance gate to `scripts/audit_external_fidelity_acceptance.py` and `scripts/self_test_external_fidelity_acceptance.py`, requiring guarded materialization provenance, `acceptance_ready=true`, a SHA40 collection commit, an ISO-like lock date, operator confirmation booleans, and strict evidence flags that remain false until manifest-backed logs/videos pass.
- Added `scripts/materialize_fidelity_acceptance.py` so the draft-to-acceptance step is now a guarded non-evidence materialization plan requiring independent-operator, real-platform, render-backed-video, real-rollout, manifest, commit, and skill-hash confirmations before any write.
- Tightened `scripts/materialize_fidelity_acceptance.py` so the write path also requires the supplied code commit and cache-independent skill-library hash to match the current clean checkout before `external_validation/fidelity_acceptance.json` can be written.
- Added `scripts/self_test_fidelity_acceptance_materializer.py` as a fidelity materializer checkout self-test for temporary clean writes, stale commit rejection, mismatched skill-hash rejection, dirty checkout rejection, cache-file exclusion, and real acceptance untouched verification.
- Added `scripts/build_external_backend_integration_packet.py` so the primary public-simulator backend blocker is converted into a non-evidence external backend integration packet with work orders and a fail-closed audit while strict backend/evidence gates remain false.
- Added `external_validation/runner/maniskill_reference_backend.py` and `scripts/audit_maniskill_backend_readiness.py` so the repository has a non-evidence ManiSkill/SAPIEN reference backend candidate that passes API/config/adapter contract checks and a synthetic MP4 writer check while official collection and strict evidence remain fail-closed.
- Hardened the ManiSkill reference backend MP4 extractor so state-shaped arrays cannot masquerade as render videos, with `scripts/audit_maniskill_backend_readiness.py` enforcing that guard.
- Added explicit render-backend/shader controls to the ManiSkill reference backend, render-video preflight, and pilot runtime liveness audit so local renderer settings are recorded rather than implicit while strict evidence remains false.
- Added `scripts/audit_maniskill_reference_collection_preflight.py` so the explicit ManiSkill reference backend/config/run-id/alias preflight is audited separately and currently reaches the fidelity-acceptance gate while collection and strict evidence remain fail-closed.
- Added `scripts/self_test_external_runner_backend.py` as an External runner backend probe self-test that exercises the actual collection runner with a temporary non-template backend, schema-valid temporary JSONL records, video writes, a diagnostic-fallback rejection case for the official video write guard, a schema-invalid record rejection case for the official JSONL write guard, and a partial-failure preservation case for atomic official evidence promotion while leaving the real manifest untouched.
- Added `scripts/audit_external_pilot_smoke.py` and `scripts/build_external_pilot_smoke_packet.py` so a real backend can be tested on a quarantined 12-row first-panel pilot before official collection, while pilot logs/videos remain excluded from external evidence.
- Added `scripts/audit_maniskill_render_video_preflight.py` so the tracked ManiSkill/SAPIEN route separately audits render-backed RGB MP4 export before official collection and records the current local renderer blocker with a renderer-failure classifier without treating diagnostic fallback videos as evidence.
- Added render/pilot progress markers to the ManiSkill render-video preflight and pilot runtime liveness audit so local timeouts report the last completed renderer or collection stage without creating evidence.
- Added failure-stage and terminal-stage diagnostics to the ManiSkill render-video preflight so the current local Vulkan blocker is reported at `initial_render_start` rather than being confused with later cleanup reaching `close_done`.
- Extended the ManiSkill render-video preflight with a renderer profile matrix over `cpu/minimal`, `gpu/minimal`, and `sapien_cuda/minimal`, so the current local Vulkan descriptor-pool blocker is recorded as attempted profile data rather than only as retest instructions.
- Added `scripts/audit_maniskill_render_resource_sweep.py` as a bounded render resource sweep that retests the first primary ManiSkill task at `16x16` across `cpu/minimal`, `gpu/minimal`, and `sapien_cuda/minimal`, recording the current descriptor-pool failure persistence as non-evidence work orders.
- Extended the ManiSkill render-video preflight with a timeout diagnosis retest so a bounded timeout can be followed by a quarantined lower-resolution retest that exposes concrete renderer failures such as `vk::Device::allocateDescriptorSetsUnique: ErrorOutOfPoolMemory` without counting diagnostic media as evidence.
- Added explicit render-preflight bounds to the full PowerShell build entry point and validator so single-command reproduction records the declared timeout and profile-matrix scope expected from the independent-validation path.
- Added `scripts/audit_maniskill_pilot_runtime_liveness.py` as a ManiSkill pilot runtime liveness audit that launches the tracked reference runner in quarantined output directories with a hard timeout; the current local report records `pilot_runtime_ready=false`, no promoted pilot row/video, explicit render-backend/shader controls, and a stage-aware video-guard failure at `record_video_start` before official collection.
- Hardened the ManiSkill pilot runtime liveness audit so it explicitly reports timeout progress and, when the runner reaches video export, whether a diagnostic sidecar rejected before JSONL write was stopped by the official video guard before any official row can be written.
- Added a reset-timeout triage sidecar to the ManiSkill pilot runtime liveness audit so reset-stage timeouts record the active row, task config hash, method alias, resolved method, primary ManiSkill env, and operator next actions without counting as evidence.
- Added backend reset substage markers to the ManiSkill reference backend and pilot liveness audit so reset-stage timeouts localize the last reached import, env construction, env reset, or initial render-capture substage before operator collection.
- Added `scripts/build_maniskill_render_machine_qualification.py` and `external_validation/render_machine_qualification_packet.md` so the exact collection machine must pass platform probing, render-backed MP4 preflight, pilot liveness, and zero diagnostic fallback videos before official collection can begin.
- Extended the render-machine qualification builder with a render failure remediation packet and CSV work orders so the current render-backed-video blocker is converted into non-evidence platform, renderer-profile, pilot-liveness, fidelity-acceptance, and collection-readiness actions.
- Added `scripts/build_external_config_manifest_packet.py` so prepared task configs are converted into a non-evidence external config manifest packet with manifest-declaration and hash-lock work orders while strict config evidence remains false.
- Added `scripts/build_external_rollout_evidence_packet.py` so the missing raw JSONL logs, videos, manifest writing, strict rollout recomputation, pairing, release, and final evidence gates are converted into a non-evidence external rollout evidence packet while strict rollout and external evidence remain false.
- Added `scripts/build_external_ablation_collection_packet.py` so the five strict external ablation requirements are converted into manifest-declared work orders over the same accepted configs, skill library, resets, observation interface, and compute budget while manifest ablation evidence remains false.
- Added `scripts/build_external_evidence_intake_ledger.py` so every current strict external-evidence failure is mapped to the required operator artifact, source packet, strict gate, and completion test while strict external evidence remains false.
- Added `scripts/build_external_method_implementation_packet.py` so the missing non-oracle baseline implementation layer is converted into concrete non-evidence work orders while strict adapter evidence remains fail-closed.
- Added `external_validation/method_reference_provenance.csv` as a reference-adapter provenance catalog that hash-lists the current non-oracle interface adapters without treating them as independent evidence.
- Added `external_validation/adapter_acceptance_fixtures.json`, `external_validation/adapter_acceptance_fixtures.md`, and `external_validation/adapter_acceptance_fixtures.csv` as non-evidence adapter acceptance fixtures for all non-oracle method implementations before strict rollout collection.
- Added `external_validation/method_manifest_cutover_checklist.csv` and `external_validation/method_manifest_cutover_checklist.md` as a non-evidence method manifest cutover checklist that maps every non-oracle method to required `manifest.methods[]` fields, checkpoint/config hashes, provenance signoff, fairness-contract bindings, and JSONL `policy_or_config_hash` matching before strict adapter evidence can pass.
- Added a strict independent method provenance gate, strict checkpoint/config artifact gate, and strict fairness-contract binding gate to the method implementation packet and strict adapter validator so real non-oracle manifests must declare independent operator/lab signoff, matched skill/observation/compute conditions, locked policy/config artifacts, and no oracle/scaffold/reference/proposed-code leakage for independent baselines.
- Added `scripts/build_external_runbook.py` so the collection schedule becomes a non-evidence operator packet with a runbook, 1,440-row record sheet, task cards, config templates, and audit.
- Updated the external collection plan and runbook audit so the operator runbook now carries 47 validation commands, the current ManiSkill route gates, and the actual high-fidelity collection command template before strict evidence gates.
- Added `external_validation/platform_qualification_checklist.md` and `scripts/audit_external_execution_readiness.py` so the independent external-validation packet is checked as executable while still remaining explicitly non-evidence.
- Added `scripts/audit_external_backend_contract.py` so the independent backend module must pass a standalone contract qualification gate before collection readiness can be trusted.
- Added `scripts/self_test_external_backend_contract.py` so strict backend qualification is tested on temporary complete, incomplete, and template backends without creating evidence.
- Added `scripts/self_test_external_fidelity_acceptance.py` so strict platform/fidelity acceptance is tested on a temporary complete high-fidelity fixture without creating evidence.
- Added `scripts/materialize_external_configs.py` so real task configs can be generated only through a guarded operator command after concrete platform and compute values are supplied.
- Added `scripts/self_test_external_config_evidence.py` so strict manifest-declared task-config evidence is tested on temporary configs without creating evidence.
- Added a strict config evidence hash gate in `scripts/validate_external_configs.py` and `scripts/self_test_external_config_evidence.py` so future manifest-declared task configs are rejected when `config_hash` is missing, malformed, or mismatched with `config_path`.
- Added `scripts/build_external_operator_packet.py` so the independent validation path has a generated go/no-go packet with pre-collection blockers, collection commands, and post-collection strict gates.
- Added `scripts/build_external_collection_job_packet.py` so the External collection job packet emits a guarded PowerShell command spine, operator checklist, packet, and audit for moving from platform/render qualification through official collection, postcollection sealing, manifest promotion, and final strict evidence gates while the current state remains no-go.
- Added `scripts/build_external_operator_handoff_bundle.py` so the independent validation path has a hash-listed non-evidence handoff manifest that excludes rollout logs, videos, checkpoints, local dry-run artifacts, placeholder media, and real evidence manifests.
- Updated the acquisition and operator packets so backend selection first runs the strict backend qualification audit before collection readiness or actual collection commands.
- Updated the operator packet so it exposes the tracked ManiSkill reference route with exact strict preflight/collection commands and the single route-specific fidelity-acceptance blocker while the official no-go state remains.
- Updated the collection-readiness audit so the same tracked ManiSkill reference route is emitted in the primary pre-collection gate, with computed non-evidence checks, exact commands, and fidelity acceptance as the single route-specific blocker.
- Updated the outreach memo, four-page preview, and Haonan/Yilun package to reflect the operator-packet/no-go stance while keeping Haonan's role framed as scientific fit and falsification advice.
- Added `external_validation/config_schema_v1.json` and `scripts/validate_external_configs.py` so task config templates and future strict evidence configs are machine-checked separately.
- Added `scripts/build_external_baseline_contract.py` so the external package has a non-evidence baseline implementation contract, method matrix, per-method adapter specs, and an audit that still marks independent non-oracle implementations as missing.
- Added `scripts/build_external_adapter_scaffolds.py` so the baseline specs produce executable scaffold templates that strict external validation still rejects as evidence until real implementations replace them.
- Added `scripts/build_external_reference_adapters.py` so every method has an executable reference adapter and a non-evidence contract audit before a real external run.
- Added `scripts/build_external_local_dry_run.py` so the frozen local suite is exported into 20,640 schema-compatible external-style JSONL records for plumbing validation while remaining explicitly non-evidence.
- Added `scripts/self_test_external_adapter_scaffold_guard.py` so scaffold-only adapter detection is itself tested.
- Added `scripts/validate_external_adapters.py` so external baseline adapters must expose the seam-model API, proposal fields, log fields, and policy/config hash reporting; strict mode remains blocked until manifest-declared real implementations replace scaffolds.
- Added `scripts/self_test_external_adapter_evidence.py` so strict manifest-declared adapter evidence is tested on temporary complete implementations while missing manifests, scaffold templates, leaky provenance, implementation-source hashes masquerading as checkpoint/config artifacts, and reference adapters remain fail-closed through a strict reference-adapter rejection gate.
- Added `scripts/build_external_manifest.py` so real or high-fidelity rollout logs, videos, configs, checkpoints, hashes, and recomputed metrics can be assembled into `external_validation/manifest.json` without hand-editing evidence.
- Added `external_validation/manifest_assembly_checklist.csv` as a manifest assembly checklist emitted by the manifest builder, mapping each required real-manifest field to current file/hash state, operator action, and strict gate while no real manifest is written.
- Added `scripts/self_test_external_manifest_builder.py` so the manifest builder's hash scan, raw JSONL loading, metric recomputation, report, checklist, and manifest-write path are exercised on a complete temporary fixture without touching the real manifest or real report.
- Added a strict manifest promotion gate to `scripts/build_external_manifest.py` and `scripts/self_test_external_manifest_builder.py` so official manifest writing is refused until pre-write config/log/video/method/release blockers are closed, and a partial method manifest is rejected in self-test.
- Added `scripts/build_external_precollection_manifest_draft.py` so prepared task-config hashes and the official-manifest cutover command spine are captured in a non-evidence External precollection manifest draft while method/log/video blockers remain explicit.
- Added `scripts/build_external_precollection_freeze_receipt.py` so the operator sheet, alias map, prepared configs, method cutover checklist, manifest draft, runner files, source audits, current commit, and skill-library hash can be frozen in a non-evidence external precollection freeze receipt before official JSONL/video collection.
- Added `scripts/self_test_external_precollection_freeze_receipt.py` so the precollection freeze receipt builder is tested on a temporary complete backend/config/operator/run-id/clean-checkout fixture while missing backend selection, placeholder run identity, missing lock artifacts, and dirty checkout remain fail-closed without touching the real receipt.
- Added `scripts/build_external_postcollection_evidence_seal.py` so raw JSONL logs, rollout videos, prepared configs, precollection receipt, and operator metadata can be hash-locked in a non-evidence External postcollection evidence seal after official collection and before manifest promotion.
- Added `scripts/self_test_external_postcollection_evidence_seal.py` so the postcollection evidence seal builder is tested on a temporary complete sealed fixture while missing operator metadata, incomplete official videos, and pre-existing manifests remain fail-closed without touching the real seal.
- Added `scripts/audit_external_postcollection_seal_consistency.py` so sealed raw-log, video, config, precollection, and metadata hashes are recomputed before manifest promotion and hash drift or unsealed official artifacts remain fail-closed.
- Added `scripts/self_test_external_postcollection_seal_consistency.py` so the postcollection seal consistency gate is tested on a temporary complete sealed fixture while hash drift and unsealed official artifacts remain fail-closed without touching the real consistency report.
- Softened the manuscript's agenda-facing wording so the paper naturally reads as a bounded skill-seam world/action model rather than a broad world-modeling claim.
- Added `scripts/self_test_external_release_package.py` so the release-package hash gate is tested on temporary complete artifacts while missing manifests, local-dry-run/template/scaffold/placeholder artifacts, staged/backup/diagnostic/fallback log-video artifacts, empty video directories, and non-MP4-like video artifacts remain fail-closed.
- Added a final release artifact hash recomputation gate in `scripts/audit_external_evidence.py` and `scripts/self_test_external_evidence_pipeline.py` so the final external evidence audit recomputes manifest-declared release artifact hashes and rejects tampered release hashes directly.
- Added confidence-gated external rollout statistics in `scripts/validate_external_rollouts.py`, `scripts/build_external_analysis_plan.py`, and the external rollout/full-pipeline self-tests so future external evidence must clear predeclared thresholds with 95% bootstrap confidence bounds, not point estimates alone.
- Added a final rollout confidence summary gate in `scripts/audit_external_evidence.py` and `scripts/self_test_external_evidence_pipeline.py` so the final external evidence audit rejects stale or tampered rollout metric summaries that omit the embedded confidence-gate results.
- Added `external_validation/log_schema_v1.json` and `scripts/validate_external_rollouts.py` so external success/utility margins, paired win rate, fixed-risk coverage, fixed-risk breach, and positive task-family count must be recomputed from raw episode logs.
- Tightened `scripts/audit_external_evidence.py` so a future evidence package is blocked unless manifest metrics agree with recomputed rollout metrics.
- Added `scripts/self_test_external_rollout_validator.py` to test the external rollout validator on temporary synthetic records without treating them as evidence.
- Added a strict MP4 video evidence gate in `scripts/validate_external_rollouts.py`, with self-test coverage in `scripts/self_test_external_rollout_validator.py` and `scripts/self_test_external_evidence_pipeline.py`, so strict external rollout validation rejects placeholder, diagnostic, staged, backup/internal, non-MP4-like, undersized, and out-of-manifest video paths.
- Added a strict full-method coverage gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL packages are rejected when the manifest omits any required external method, including required non-oracle baselines.
- Added a strict rollout sample-count gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL packages are rejected when manifest-declared `episodes_per_method` is below 30 or per-task/per-method record counts differ from the manifest declaration.
- Added a strict paired-panel gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL packages are rejected when paired reset panels contain duplicate method rows or are missing manifest-declared methods.
- Added a strict rollout uniqueness gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL packages are rejected when they contain duplicate episode identities or reused rollout video paths.
- Added a strict task-config hash gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL rows are rejected when manifest-declared task configs are stale, missing, hash-mismatched, or inconsistent with row skill/risk fields.
- Added a strict policy/config hash gate in `scripts/validate_external_rollouts.py` and `scripts/self_test_external_rollout_validator.py` so future raw JSONL rows are rejected when `policy_or_config_hash` does not match the manifest-declared method `checkpoint_or_config_hash`.
- Added an official video write guard in `external_validation/runner/real_collection_runner.py` so actual collection rejects diagnostic fallback sidecars, non-MP4-like files, undersized files, out-of-dir paths, or unexpected returned video paths before any official JSONL row is written.
- Added an official JSONL write guard in `external_validation/runner/real_collection_runner.py` so actual collection calls the strict rollout-record validator and rejects schema-invalid rows before append.
- Added atomic official evidence promotion in `external_validation/runner/real_collection_runner.py` so selected-batch videos are staged as `.staging.mp4` files and official videos/logs are replaced together only after the batch succeeds.
- Added `scripts/self_test_external_collection_preflight.py` to test the external collection-readiness preflight on a temporary complete fixture without overwriting the real readiness report or treating the fixture as evidence.
- Added `scripts/self_test_external_pairing_integrity.py` so paired-reset fairness is tested on temporary complete method panels while missing manifests, duplicate rows, incomplete panels, and terminal-sample mismatches remain fail-closed.
- Added `scripts/self_test_external_evidence_pipeline.py` to test the full strict external-evidence path on a temporary synthetic manifest/config/log/video/checkpoint package without creating real repository evidence.
- Added `results/external_evidence_pipeline_self_test.json` and `results/external_evidence_pipeline_self_test.md` so the full strict external-evidence path self-test leaves durable proof that the synthetic package reaches final readiness, tampered rollout confidence summaries and release hashes fail, and real repository evidence reports remain untouched.
- Updated GitHub validation so the rollout-validator and full-pipeline evidence self-tests run as first-class CI steps instead of only being covered by the local build and artifact validator.
- Fixed strict evidence-audit metric parsing so legitimate zero-valued fixed-risk breach is accepted as a number rather than treated as missing.
- Added `docs/claim_evidence_ledger.json` and `scripts/audit_claim_boundary.py` so the package fails validation if it overclaims beyond the bounded local evidence.
- Added `scripts/audit_submission_readiness_gap.py` so the active objective is tracked as concrete satisfied, missing, and human-polish requirements without falsely declaring the paper complete.
- Added `scripts/audit_visible_contribution.py` so README, final audit, readiness docs, version log, child status, and outreach package must stay synchronized with the current materializer/operator-packet/outreach state.
- Added `scripts/build_reviewer_response_packet.py` and `docs/reviewer_response_packet.md` so the reviewer response packet maps hostile reviewer objections to current evidence, allowed claims, remaining gates, and Haonan/Yilun outreach use without changing STRONG_REVISE.
- Added `scripts/audit_local_falsification.py` and manuscript integration for abstention, cost/search, risk-monotonicity, slice-coverage, and oracle-gap checks over episode-level hard-slice rows.
- Added `scripts/audit_holdout_robustness.py` and manuscript integration for task-family, seam-regime, split, task-regime, and hash-fold withheld-slice local robustness checks.
- Added `scripts/audit_diagnostic_mechanism.py` and manuscript integration for exported diagnostic labels, seam decisions, and planner-edge updates over local rows.
- Added `scripts/audit_decision_quality.py` and manuscript integration for comparative decision quality: accepted-seam coverage, non-abstain quality, recovered predecessor-abstained accepts, and shared-abstention breach changes over local hard rows.
- Added `scripts/audit_planner_edge_policy.py` and manuscript integration for planner-edge policy quality: local hard-slice planning frontiers, executable-edge coverage, selected-edge utility/success/breach, and positive task/regime/split margins while preserving the external-evidence boundary.
- Added `scripts/audit_failure_memory_adaptation.py` and manuscript integration for failure-memory adaptation: local observed-to-held-out seam signatures test whether early diagnostic/update memory predicts later breach and utility while preserving the external-evidence boundary.
- Added `scripts/build_local_model_release.py` and `docs/local_model_release.md` so the deterministic v5 skill-seam world/action model has a hash-locked local release card without pretending to be a trained robot policy checkpoint or external evidence.
- Added `scripts/audit_seam_prediction_calibration.py` and manuscript integration for local predicted seam-risk calibration against realized breach, including ECE10, max bin gap, monotone risk deciles, and decision relevance.
- Added `scripts/audit_manuscript_numbers.py` so manuscript margins, row counts, generated table values, local falsification numbers, diagnostic mechanism numbers, decision-quality numbers, planner-edge policy numbers, predictive-calibration numbers, and holdout robustness numbers must match generated result files.
- Added `scripts/audit_related_work.py` so citation coverage, novelty-boundary rows, and outreach/validation boundary wording are machine-checked across 12 required areas.
- Added `scripts/audit_reference_integrity.py` so required BibTeX fields, DOI/arXiv identifiers, recent primary-source metadata, and Haonan/Yilun-adjacent author coverage are machine-checked.
- Added `scripts/audit_manuscript_readability.py` so the central framing, novelty boundary, contact-as-testbed positioning, paragraph readability, and stale manual-polish blocker are machine-checked.
- Added `scripts/audit_presentation_quality.py` so the compiled PDF/source/log/figure surface is checked for top-conference presentation hygiene.
- Added `scripts/audit_figure_readability.py` so all seven main figure companions are checked for render resolution, foreground density, contrast, edge margins, color detail, and manuscript references.
- Added `scripts/audit_camera_ready_design.py` so all 30 rendered PDF pages are checked for nonblank content, density, contrast, margins, sparse-page count, canonical PDF parity, and selected text anchors.
- Added `scripts/build_submission_artifacts.ps1` as the single local rebuild command for experiments, manuscript, PDF, audits, and outreach artifacts.
- Terminal decision remains STRONG_REVISE.
- ICLR main readiness remains no pending real robot or accepted high-fidelity validation, released checkpoints/logs, hardware videos, and manifest-declared independent baseline evidence.
