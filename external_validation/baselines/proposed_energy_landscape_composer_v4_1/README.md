# Adapter Scaffold: proposed_energy_landscape_composer_v4_1

Not external evidence: `true`.
Scaffold only: `true`.

Role: previous proposed method baseline.
Required entrypoint: `compose_with_v4_1_rules`.

Replace `adapter_template.py` with an independent implementation before this method can appear in `external_validation/manifest.json` as evidence.

## Allowed Inputs

- same energy estimates
- current observation
- shared skill library

## Forbidden Advantages

- v5-only repair memory
- v5 fixed-risk calibration updates
